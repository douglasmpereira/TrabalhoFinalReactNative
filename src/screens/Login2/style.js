import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    input: {
     borderWidth: 0.2,
     borderColor: "grey",
     width: 250,
     height: 30,
     marginTop: "5%",
    },
    view: {
      flex: 1,
      justifyContent:"center",
      alignItems: 'center',
    },
    button: {
        borderWidth: 0.2,
        borderColor: "grey",
        padding: '2%',
        marginTop: '5%',
        borderRadius: '10%',
    },
});